#pragma once

#include <QGraphicsView>

class BoardView : public QGraphicsView
{
    Q_OBJECT
public:
    explicit BoardView(QGraphicsScene* scene, QObject *parent = 0);
    
signals:
    
public slots:
    
};
